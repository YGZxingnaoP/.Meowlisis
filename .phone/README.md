# 喵呜手机界面（.phone）

不改原项目的独立手机端：手机浏览器打开网页，看桌宠 + 直接语音说话。桌宠**自适应手机屏幕**（不再超出画面），支持**自定义昵称**（本地记住，不再固定"手机用户"）、**拖拽移动**与**对话区折叠**，界面为简洁中性风格（无彩色按钮）。

## 原理

- `.phone/serve.py` 起一个独立 HTTPS 服务（端口 8443）：
  - 提供手机页面（`static/`）
  - 把 `.desktopet` 目录暴露为 `/pet/`，供 iframe 加载桌宠（同源，避免混合内容）
  - 反向代理到主程序 `api.py`（127.0.0.1:1800），解决手机端跨域
- 手机页面「按住说话」：浏览器采集麦克风 → 重采样为 16k 单声道 int16 PCM → 转发到主程序 `/audio/send` → SenseVoice 识别 → LLM。
- 手机页面也可打字，走 `/chat`；AI 回复文字通过轮询 `/chatreply` 显示。

## 前置条件

1. 电脑端启动主程序 `api.py`（1800）。
2. 电脑端启动 GPT-SoVits（`.Sovits`，9880）——否则 AI 不会说话（声音在电脑端播放）。
3. 若要用「按住说话」语音识别，还需：
   - `config.yml` 中 `sensevoice.enabled: true`（默认已开启）
   - 启动 `.SenseVoice` 服务端（配置 GUI 有「启动 SenseVoice」按钮）

> 说明：AI 回复的**声音在电脑端播放**（mpv 本地播放），手机端只显示文字和接收语音输入；这是现有项目架构决定的，本文件夹未改动原项目。

## 启动

在项目根目录 `D:\.Meowlisis` 下：

```bat
runtime\python.exe .phone\serve.py
```

首次启动会自动生成自签名证书到 `.phone/cert/`。

## 手机访问

1. 手机与电脑连**同一局域网**。
2. 查看电脑局域网 IP（cmd 执行 `ipconfig`），例如 `192.168.1.5`。
3. 手机浏览器打开：

```
https://192.168.1.5:8443
```

4. 首次访问会提示「连接不是私密连接」，点 **高级 → 继续前往**（Android）或 **显示详细信息 → 访问此网站**（iOS）。
5. 点「按住说话」并允许麦克风权限，说话时按住、松开发送。

## 使用

- **自定义昵称**：左上角「我是 手机用户」→ 点击直接输入昵称、回车保存；首次访问只提示一次（**不再自动弹出输入框**，避免打字/回车误进昵称框导致消息发不出）。昵称保存在**本机浏览器 localStorage**（同一手机/浏览器只需设一次），发文字消息时以该昵称身份发给小猫，小猫会为这个名字建用户档案。
- **桌宠自适应**：桌宠会按手机屏幕自动缩放、完整显示在画面上，不再超屏；右上角 ＋/－/⟲ 可手动微调大小或重置（重置 = 恢复默认大小并回到居中）。
- **拖拽桌宠**：手指/鼠标**按住桌宠拖动**即可移动位置；**双击桌宠回中**。旋转屏幕、缩放后位置保持不变。
- **折叠对话**：聊天区顶部有一条细把手（▾ 收起对话 / ▴ 展开对话），点击可**收起/展开对话框**；收起后桌宠画面自动放大到整屏。状态会被记住（刷新后保持）；折叠时**手动发消息会自动展开**对话区。
- **按住说话**：按住按钮录音，松开发送，语音送 SenseVoice 识别后进入 LLM。
- **打字发送**：输入框打字回车或点「发送」。发送失败（如主程序未启动）会**在对话区显示具体原因**。
- **AI 声音**：点 AI 气泡旁的 🔊 可在手机端播放合成语音。

> ⚠️ **语音与昵称的关系（重要）**：文字消息使用你设置的昵称；「按住说话」的语音消息仍由**服务端声纹系统**判定说话人（`config.yml → sensevoice.target_speakers`，命中才处理），与页面昵称相互独立。若想让语音也以你的昵称进入对话，需把对应声纹名加入 `target_speakers`（或临时关闭 inject 源 `speaker_verify`），手机端不代改主程序。

## 常见问题

- **麦克风无法授权**：必须用 `https://` 访问（http 会被浏览器拒绝 getUserMedia）。自签名证书要点「继续前往」后才可用麦克风。
- **语音发送了但 AI 没反应**：确认 SenseVoice 服务端已启动、主程序 `api.py` 已启动；或改用文字输入验证链路。
- **文字发了（气泡出现）但收不到 AI 回复文字**：主程序收到消息后，回复文字需要经 **GPT-SoVits（`.Sovits`，9880）合成流程**才会回传到手机轮询接口。请先确认 GPT-SoVits 已启动、电脑端能正常开口说话；手机端再刷新重试。（本文件夹不改原项目，不代做“无语音也能回文字”的改动。）
- **声纹过滤**：SenseVoice 识别结果会走 `target_speakers` + `speaker_threshold` 校验，若被过滤可调整 `config.yml`（原项目配置，本文件夹不代改）。
- **手机听不到 AI 声音**：正常现象（AI 声音默认在电脑端 mpv 播放）。如需在手机端试听，可点 AI 消息气泡旁的 🔊。
- **端口被占用**：修改 `serve.py` 顶部 `PORT` 即可（默认 8443）。

## 目录结构

```
.phone/
├── serve.py          # HTTPS 服务 + 反向代理 + 证书生成
├── static/
│   ├── index.html    # 手机界面
│   ├── style.css     # 样式
│   └── app.js        # 录音上传 / 文字对话 / 轮询回复
└── cert/             # 自签名证书（首次启动自动生成）
```
